{
  "name": "@duo/coord-server",
  "version": "0.1.0",
  "description": "BroCode Phase 2 coordination server \u2014 NestJS gateway owning presence, pairing, per-agent queues, locks and the cross-domain handshake",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "brocode-coord": "dist/main.js"
  },
  "files": [
    "dist",
    "dashboard"
  ],
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "start": "npm run build && node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@duo/coord-client": "^0.1.0",
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-express": "^10.4.15",
    "@nestjs/platform-socket.io": "^10.4.15",
    "@nestjs/websockets": "^10.4.15",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "socket.io": "^4.8.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@swc/core": "^1.15.47",
    "socket.io-client": "^4.8.1",
    "unplugin-swc": "^1.5.9"
  }
}
